<div class="row">
    <div class="col-auto">
        <h2> Welcome {{name}}! View your cart </h2>
    </div>
</div>
<div>
{{#each products as |product| }}
    <div class="row align-center mb-5">
  <div class="col-md-6">
      <h2>
          <a href="/product/{{id}}">{{name}}</h2>
          </a>
    </div>
  <div class="col-md-6">
       <img src="/images/{{product.name}}.jpg" alt="{{this.description}}" />
    <p>Price:${{price}}</p>
     <button type="button" class="btn btn-primary">
          Remove from cart 🗑
      </button>
</div>
{{/each}}
<div class="row mt-4">
  <div class="col-md-6">
      <button type="button" class="btn btn-primary">
          Checkout cart
      </button>
      <a href="/">
      <button type="button" class="btn btn-primary">
          Continue shopping
      </button>
      </a>
  </div>
</div>
    
